/* ===========================
   MAIN — Entry Point
=========================== */
import { createGameController } from './controllers/GameController.js';

const controller = createGameController();
controller.init();
